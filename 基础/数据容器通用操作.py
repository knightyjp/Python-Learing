"""
max最大元素："max(容器名称)"
min最小元素："min(容器名称)"
tips:滚轮键可矩形选中
容器的通用转换功能：list(容器) 将给定容器转换为列表
                str(容器) 将给定容器转换为字符串
                tuple(容器) 将给定容器转换为元组
                set(容器) 将给定容器转换为集合
容器通用排序功能： sorted(容器, [reverse = True])
               对容器中内容进行一个反向的排序(从大到小)，如果不要", [reverse = True]"则是从小到大的正向排序
"""